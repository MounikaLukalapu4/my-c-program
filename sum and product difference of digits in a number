#include<stdio.h>
int main()
{
	int n,rem,sum=0,pro=1,temp;
	scanf("%d",&n);temp=n;
	while(n>0)
	{
		rem=n%10;
		sum=sum+rem;
		n=n/10;
	}
	while(temp>0)
	{
	rem=temp%10;
	pro=pro*rem;
	temp=temp/10;
	}
	printf("%d",pro-sum);
	return 0;
}
