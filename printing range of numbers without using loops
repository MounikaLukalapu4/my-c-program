
#include <stdio.h>
int numbers(int n,int m);
int main()
{
    int n,m;
  scanf("%d %d",&n,&m);    
  numbers(n,m); 

    return 0;
}
int numbers(int x,int y)
{
    if(x<=y)
    printf("%d\n",x);
    x++;
    numbers(x,y);
    
}
