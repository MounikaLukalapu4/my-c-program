#include<stdio.h>
int main()
{
    int arr[20],i,j,div=0;
    for(i=0;i<5;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=0;i<5;i++)
    {
	
    for(j=2;j<arr[i];i++)
    {
        if(arr[i]%j!=0)
        div++;
    }
    if(div==2)
    printf(" \t %d",arr[i]);
    }
    return 0;
}
